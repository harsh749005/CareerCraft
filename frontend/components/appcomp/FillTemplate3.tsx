// import { fillResumeTemplate } from "./resumeTemplateEngine";

/**
 * Classic single-column layout with centered name, double-rule section titles,
 * and company+date on the same row (matches `template3.js`).
 *
 * Uses the "classic" variant of the shared template engine so that:
 *  - Experience renders as: Company (left) | Date (right), Job Title bold below
 *  - Education renders as: Institution: Degree (left) | Duration (right)
 *  - Skills render as flat uncategorized or categorized inline text
 */
// export function fillTemplate3(template: any, formData: any) {
//   return fillResumeTemplate(template, formData, "classic2");
// }