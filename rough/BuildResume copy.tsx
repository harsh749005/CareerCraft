// import React from "react";
// import BuildResume from "@/components/screens/BuildResume";
// import { useLocalSearchParams } from "expo-router";

// const BuildResumeRoute = () => {
//   const { resume_id } = useLocalSearchParams<{ resume_id?: string | string[] }>();
//   const resumeId = Array.isArray(resume_id) ? resume_id[0] : resume_id;

//   return <BuildResume resumeId={resumeId} />;
// };

// export default BuildResumeRoute;

